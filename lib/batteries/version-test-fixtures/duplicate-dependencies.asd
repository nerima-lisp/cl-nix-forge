;; Source order is preserved and duplicates are not collapsed: the list is
;; reported as written, so a redundant entry stays visible to the caller.
(defsystem "duplicated"
  :depends-on ("cl-weave" "cl-date-kit" "cl-weave"))
