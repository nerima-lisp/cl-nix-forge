## [0.5.3] - 2026-07-29

- Lives at the repository root, outside `docs/`, on purpose: the repo-rooted
  docs site snippet-includes this file, which only resolves when
  `mkDocsSite`'s `fileset` escape hatch reaches outside the docs directory.
- FORGE-PRESET-CHANGELOG-MARKER
