# forge-demo

Documentation for the `examples/flake-outputs` demo package, built by
`mkDocsSite` with a caller-supplied `pname`, `version` and `meta` rather than
the generic `docs-site`/`unstable` identity the builder used to hardcode.
